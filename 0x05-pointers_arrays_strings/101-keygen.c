#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * keygen password generator
 *
 * this function generate a valid password
 */
int main(void)
{
    srand(time(NULL)); /* Initialize the random number generator with the current time */

    printf("Tada! Congrats\n");

    return 0;
}

